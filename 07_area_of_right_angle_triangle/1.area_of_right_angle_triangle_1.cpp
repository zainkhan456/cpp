#include<iostream>
using namespace std;
int main()
{
	float base,height,area;
	cout<<"enter the value of base and height of the right angle triangle: "<<endl;
	cin>>base>>height;
	area = (base * height) * 0.5;
	cout<<"the area of the right angle triangle is "<<area;
	return 0;
}
